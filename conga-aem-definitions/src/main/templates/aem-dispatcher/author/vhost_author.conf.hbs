{{ensureProperties
    "httpd.serverName"
~}}

# Virtual host for author (HTTP)
<VirtualHost *:80>

{{#if httpd.ssl.enforce}}
  # Redirect all requests to SSL
  Redirect permanent / https://{{httpd.serverNameSsl}}/
{{/if~}}

{{> aem-dispatcher/author/vhost_author.partials.hbs}}

</VirtualHost>
